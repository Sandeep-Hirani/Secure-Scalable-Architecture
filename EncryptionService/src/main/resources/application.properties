spring.application.name=EncryptionService
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=DEBUG
logging.level.root=DEBUG

# Configure the encryption backend:
#  - Set encryption.kms-key-id (and rely on AWS_* credentials) to use AWS KMS.
#  - Or set encryption.local-key to a base64-encoded AES key for local development.
# encryption.region=us-east-1
